# 项目协作规范

## 项目需求与核心功能

本项目是 AI 简历助手，目标是在不破坏既有简历编辑能力的前提下，完成简历导入、结构化编辑、版本管理、智能诊断与求职辅导。

核心功能包括：

- 维护主简历与岗位版本，支持创建、复制、导入和切换。
- 使用解析 API 导入 PDF/图片简历并注入项目模板，同时保留原版文件供查看。
- 提供模板、样式、模块排序、PDF/DOCX 导出等排版能力。
- 分离配置对话 API 与解析 API，并通过统一网关适配直连或中转站接口。
- 支持简历问答、诊断、深度追问、JD 分析、修改建议、预览确认和保存。
- Harness 负责上下文组装与压缩、分层记忆、工作流状态、持久化和可观测性。

任何改动都必须优先保证现有功能、数据兼容性和用户体验不退化。

## 协作判断原则

- 对用户的指令可以进行适当思考，不机械执行；如发现需求矛盾、潜在风险、信息缺口或更合适的实现方式，应及时向用户指出并说明理由。
- 思考与建议不得擅自扩大用户授权范围；涉及重要取舍时，应先与用户确认。
- 每次修改后端后，为了方便用户验收，请重启后端。此外，执行功能构建/修改后，给出验收标准。

## 关键文档

- `README.md`：项目概览、功能、架构、启动方式和接口说明。
- `docs/README.md`：项目文档索引。
- `docs/agent-architecture.md`：Agent 架构与状态边界；实际行为以当前代码为准。
- `docs/testing.md`：功能回归测试清单。
- `docs/source-development-testing.md`：源码开发/测试，以及单用户和多用户 Windows 脚本流程。
- `docs/windows-single-user-installation.md`：Windows 单用户安装包、启动、数据和卸载说明。
- `docs/docker-multi-user-deployment.md`：多用户 Docker 部署与运维说明。
- `.env.example`：环境变量示例，不得把真实密钥写入仓库。

## 必须遵循的执行流程

所有代码、配置、文档或文件树变更必须严格遵循：

1. **Plan**：先阅读相关代码与文档，说明目标、范围、风险、兼容要求和测试方法。
2. **Execute**：只修改已确认范围，保留无关改动、运行数据和回退能力。
3. **Test**：执行与风险匹配的自动测试、构建或健康检查，明确报告失败和未覆盖项。
4. **用户验收**：提交前交由用户检查；未通过则继续修复并重新测试。
5. **Commit**：仅在用户明确验收或明确要求提交后创建提交，不提前提交。

## 删除与清理约束

- 删除任何文件夹前，必须先列出准确路径、用途、引用检查、影响和恢复方式，并获得用户明确同意。
- 文件是否“当前未引用”不能单独作为删除理由；还要判断测试、诊断、部署、文档、资产生成和后续维护价值。
- 不得删除运行依赖、正式测试、用户数据、数据库、上传原文件、密钥配置、部署配置或回滚所需内容。
- 缓存、日志、构建产物等可再生内容也应先确认未被当前进程或部署流程使用。
- 清理后必须复查 Git 变更范围并执行必要测试，随后等待用户验收再提交。
