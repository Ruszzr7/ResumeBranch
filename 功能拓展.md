# 功能拓展

> 状态：阶段 0—5、阶段 6 的功能部分、双 API/统一网关、无损解析注入及导入/排版完善均已完成并通过自动验收。具体生产部署拓扑、等价环境压力验证、原版式保留及旧链路删除按约定后续单独实施。  
> 目标：在不影响现有对话、修改、确认、撤回、导出和多任务隔离的前提下，升级 Agent Harness，并实现可持续、可恢复的简历“拷打”功能。

## 一、架构原则

采用三层状态边界：

- **业务数据库**：简历、JD、布局、待确认修改、修改版本与撤回记录，是唯一业务事实来源。
- **LangGraph Checkpointer**：仅保存工作流状态，如当前模式、拷打阶段、关注模块、当前问题和暂停/恢复位置。
- **Memory Manager**：管理近期完整对话、滚动摘要、用户偏好和已验证事实，并按 token 预算组装上下文。

Checkpointer 不保存完整简历、JD、附件或确认候选，Graph 节点运行时始终从业务数据库读取最新数据，避免双重事实来源。

## 二、不可破坏的现有行为

- `/chat` 请求方式和 SSE 事件保持向后兼容。
- 普通咨询、明确字段修改、复杂修改的现有路由效果保持不变。
- 所有简历写入仍必须经过临时预览和用户确认。
- 选择性接受、拒绝、哈希校验和撤回机制保持有效。
- 简历、JD、布局始终以当前数据库内容为准。
- 用户、主简历和岗位版本之间继续严格隔离。
- 旧任务没有新状态时可以正常加载并自动兼容。

## 三、阶段性任务

### 阶段 0：锁定基线（已完成）

- 为现有路由、上下文拼接、SSE、确认和撤回补充特征测试。
- 以当前全部后端测试和前端生产构建作为回归基线。
- 记录现有接口及确认数据结构，后续不得非兼容修改。

阶段 0 基线记录（2026-08-04）：

- 后端全量测试：70 项通过，其中新增 6 项 Harness 特征测试。
- 前端生产构建：通过，Vite 完成 1460 个模块转换；保留现有大分块告警，不在本阶段处理。
- 新增基线测试文件：`tests/test_harness_baseline.py`。
- 已冻结的上下文契约：系统规则与最新简历/JD 在首条 SystemMessage；历史工具结果和确认标记不发送给 LLM；照片 Base64 不进入系统上下文。
- 已冻结的压缩契约：当前实现生成一条 SystemMessage 摘要，并保留压缩输入中的最后 5 条消息。
- 已冻结的普通对话 SSE 顺序：`stream* → final → end`。
- 已冻结的修改预览 SSE 顺序：`building_preview → validating → ready → confirm → final → end`。
- 已冻结的确认载荷：包含确认 ID、候选简历/布局、修改项、基线哈希、工具参数和任务 ID。
- 已冻结的成功响应：确认接口返回 `success/message/action/resume_data`；撤回接口返回 `success/message/resume_data/layout_config`。
- 本阶段仅新增测试与文档记录，未修改生产业务逻辑。

### 阶段 1：行为等价重构（已完成）

- 将上下文构建、记忆管理和状态持久化从 `main.py`、`resume_agent.py` 中提取为独立 Harness 模块。
- 本阶段保持上下文内容、消息顺序和路由行为不变。
- 清理未实际启用的 `MemorySaver` 和失效注释。

阶段 1 完成记录（2026-08-05）：

- 新增 `backend/harness/context.py`，统一负责系统上下文注入、历史消息过滤和模型消息排序。
- 新增 `backend/harness/memory.py`，承接现有压缩配置、摘要 Prompt、窗口保留和压缩状态协调。
- 新增 `backend/harness/persistence.py`，承接附件过滤、上下文序列化、压缩触发和回合落盘。
- `main.py` 保留原函数兼容入口，`resume_agent.py` 保留原 Graph 节点与路由，只将具体 Harness 工作委托给新模块。
- 已移除未使用的 `MemorySaver` 导入，并修正“已使用 Checkpointer 持久化”的失效说明；本阶段未接入 Checkpointer。
- 新增 2 项 Harness 模块测试，锁定附件过滤及原有压缩落盘窗口。
- 后端全量测试：72 项通过；前端生产构建通过。
- 未改变数据库结构、Prompt 内容、压缩算法、SSE 契约、确认载荷和保存时机。

### 阶段 2：分层记忆与可靠持久化（已完成）

- 保留最近若干完整“用户—助手”对话对。
- 改为基于 token 预算的增量摘要，不再只按用户消息数量压缩。
- 摘要只作为不可执行的记忆数据，不作为可覆盖系统规则的指令。
- 增加状态版本号和乐观锁，防止并发请求覆盖新状态。
- 在发送 SSE `end` 前完成必要状态落盘，消除异步保存竞争。

阶段 2 完成记录（2026-08-05）：

- 新增独立的 `agent_memory_states` 表，按任务/会话隔离保存滚动摘要、近期完整消息和乐观锁版本；未改写现有任务表字段。
- 旧任务首次读取时从 `compressed_context` 懒兼容，首次成功落盘后自然进入新结构；迁移期继续镜像旧字段，便于回退。
- 近期窗口改为可配置的 token 估算预算，默认 6000 token，至少保留最近 5 个完整回合，不再拆分“用户—助手”对话。
- 摘要由“旧摘要 + 本次淘汰的完整回合”增量生成；摘要失败时保留全部未压缩消息，不丢失上下文。
- 摘要以“不可信历史数据”区块注入 SystemMessage，对标签字符进行转义，明确不得覆盖当前简历、JD 和系统规则。
- 记忆写入使用 `expected_version` 乐观并发控制；过期请求会拒绝覆盖新状态，撤回操作也会推进记忆版本。
- 回合保存由后台 `create_task` 改为在 `final/end` 之前等待完成；保存失败会在结束前发送可重试的 `persistence_error`，前端显示提示。
- 删除会话、删除任务/项目和过期会话清理已同步处理新记忆状态。
- 新增 7 项阶段 2 测试，覆盖旧数据兼容、版本冲突、完整回合窗口、摘要安全注入、摘要失败降级和 SSE 失败时序。
- 后端全量测试：79 项通过；前端生产构建通过；仅保留既有大分块告警。
- 本阶段未引入 LangGraph Checkpointer，Graph 的路由、确认载荷和成功路径 SSE 契约保持不变。

### 阶段 3：接入 LangGraph Checkpointer（基础设施已完成）

- 建立精简的可持久化 `WorkflowState`，只保存工作流控制状态和业务数据引用。
- 使用 `user_id + task_id` 构造隔离的 `thread_id`。
- 单机环境可使用 SQLite Saver；多容器/多服务器生产环境使用共享 PostgreSQL Saver。
- 为暂停、继续、刷新恢复和服务重启恢复提供持久化原语；实际拷打状态转移在阶段 4 接入。

阶段 3 完成记录（2026-08-05）：

- 新增 `backend/harness/workflow.py`，建立独立的精简 `WorkflowState` 控制图，并使用 LangGraph `SqliteSaver` 持久化。
- 当前执行图 `AgentState` 仍是无 Checkpointer 的单请求执行面；持久化控制图只保存模式、状态、阶段、关注模块、当前问题、最后节点、回合计数和业务引用。
- Checkpointer 的可写字段使用白名单校验，写入简历、JD、对话消息、附件或确认候选会被拒绝，避免建立第二业务事实源。
- 统一使用 `user:{user_id}:task:{task_id}` 作为执行和 checkpoint 的 `thread_id`，不同用户及岗位任务严格隔离。
- Checkpointer 由 FastAPI lifespan 管理，默认落盘到 `data/langgraph_checkpoints.sqlite`；Docker 环境会复用现有 `data` 持久化卷。
- 新增 `AGENT_CHECKPOINTER_ENABLED` 和 `AGENT_CHECKPOINT_DB_PATH` 配置；Checkpointer 启动失败时不阻断既有简历功能。
- 每轮开始时恢复并登记控制状态，每轮 `final/end` 前记录最后节点及状态；提供暂停、继续、删除控制原语。
- 已在管理器层验证同一 `thread_id` 在重建管理器后可恢复模式、阶段、问题和暂停状态；当前业务流程尚未自动写入真实拷打问题与阶段，该部分属于阶段 4。
- 业务记忆保存失败继续使用可重试的 `persistence_error`；仅工作流进度保存失败时使用不要求重复提交的 `workflow_error`。
- 新增 4 项 Checkpointer 测试，覆盖重启恢复、暂停/继续、用户/任务隔离、业务载荷拒绝和删除清理。
- 后端全量测试：83 项通过；前端生产构建通过；成功路径 SSE 事件契约保持不变。
- 当前仓库的 Docker 部署配置是“1 个后端容器 + 6 个 Gunicorn Worker 进程”，6 个进程通过同一 `data` 卷共享 SQLite checkpoint 文件。这在低并发下可用，但不等于单进程，仍需验证锁等待和同一任务并发写入。
- 多容器/多服务器部署时，如各实例使用本地 SQLite，checkpoint 会直接分裂；此时必须切换共享 PostgreSQL Saver。

### 阶段 4：实现拷打状态机（已完成）

- 明确 `idle/diagnosing/questioning/synthesizing/awaiting_apply/paused/completed` 等状态及合法转移，同一 `request_id/action` 必须幂等。
- 支持全面诊断、逐项拷打、JD 匹配分析、暂停、继续和结束。
- 自动定位最高优先级短板，每轮只问一个问题。
- 按背景目标、个人动作、决策权衡、技术/业务深度和量化结果逐步补全。
- 为已验证事实保存来源类型、用户原始回答/消息引用和所属简历模块；区分简历已有事实、用户明确补充和模型推测。
- 模型推测、无来源的数字、技能和经历不得进入已验证事实或修改候选。
- 每个状态节点运行时从业务数据库重新读取当前简历/JD，不依赖 checkpoint 中的历史副本。
- 信息充分后生成“原文—建议稿—修改依据”，用户明确要求应用后复用现有确认链路。
- 应用完成后可继续下一项短板，不建立第二套保存机制。

阶段 4 完成记录（2026-08-05）：

- 新增 `backend/harness/interview.py` 和 Graph 节点 `interview_coach`，支持全面诊断、逐项拷打及 JD 对照；普通聊天、明确修改和确认点击仍走原入口。
- 工作流实际使用 `diagnosing/questioning/synthesizing/awaiting_apply/completed` 阶段及 `active/paused/awaiting_confirmation/completed` 状态；LLM 调用前、输出后和控制动作后均更新控制投影。
- 每轮最多输出一个问题；暂停、继续、结束是确定性动作，不调用 LLM，非法或重复动作返回无副作用提示。
- 同一 `request_id` 重放不会增加回合计数；并发回合由管理器串行化，记忆继续使用乐观版本拒绝过期覆盖。
- `AgentMemoryState.interview_memory` 保存已核实事实、未完成项、拒绝建议和最近建议，并与对话记忆在同一版本中原子推进。
- 已核实事实的 canonical claim 直接使用本轮用户原话引用；引用必须能在当前回答逐字定位，模型改写不升级为事实，claim 中数字必须存在于引用。
- Checkpointer 仍只保存模式、阶段、聚焦模块、当前问题和回合元数据；简历/JD 每次由 `/chat` 从业务数据库加载。
- 信息充分时输出“原文—建议—依据”；“应用建议”仅生成现有 `pending_confirmation`，确认前不写简历，确认后仍使用原保存、哈希、版本和撤回链路。
- 结构化输出非法或模型异常时返回只读安全降级问题，不产生事实、候选或简历写入。

### 阶段 5：前端结构化模式（已完成）

- 保留“全面诊断、开始拷打、对照 JD”入口。
- 在兼容自然语言 Prompt 的同时，增加可选 `interaction_mode` 和 `action` 字段。
- 新字段缺省时必须继续走现有路由；页面刷新后从后端恢复的控制状态重建 UI，不以前端本地状态为真相源。
- 展示当前模块、拷打维度、已验证事实数量及暂停/继续操作。
- 不改变原有确认框和简历预览交互。

阶段 5 完成记录（2026-08-05）：

- 三个快捷入口保留原自然语言 Prompt，同时发送可选 `interaction_mode/action`；字段缺省时旧请求契约不变。
- 新增工作流状态条，展示模式、运行/暂停/待确认/结束状态、聚焦模块和已核实事实数量。
- 支持暂停、继续、结束与应用建议；应用后复用原确认框和右侧临时预览。
- 新增 `GET /tasks/{task_id}/workflow`，刷新后从后端 Checkpointer 与事实记忆恢复 UI，不使用 localStorage 伪造工作流真相。
- 新增 `workflow_state` SSE 仅在结构化拷打链路发送；普通对话继续保持 `stream* → final → end` 的原契约。
- Vite 代理目标支持 `VITE_API_TARGET`，默认仍为 `http://localhost:8000`，便于使用隔离后端执行端到端测试。

### 阶段 6：灰度切换与旧链路清理边界（功能部分已完成）

- 通过特性开关先 shadow 验证，再按用户或任务灰度启用。
- 新链路异常时可立即回退旧 Harness。
- 执行与最终生产拓扑相匹配的多进程/多实例压力、同任务并发和重启恢复测试；旧 6 Worker 配置只作为兼容场景。
- 如最终保留单机 SQLite，根据目标拓扑压测结果配置 WAL、`busy_timeout` 与监控；如扩展到多容器/多服务器，先接入并验证 PostgreSQL Saver。
- 补充 checkpoint 保留/清理策略、故障注入和可观测指标，不记录完整简历或过量个人信息。
- 稳定后停止双写，删除旧消息数阈值压缩、全局压缩状态和冗余异步保存代码（按本次约定延后）。
- 旁路仅用于迁移，最终统一到新 Harness；本轮为确保可回退，明确保留旧链路。

阶段 6 本轮完成记录（2026-08-05）：

- 新增 `INTERVIEW_HARNESS_ENABLED`、稳定 user+task 灰度桶 `INTERVIEW_HARNESS_ROLLOUT_PERCENT` 和不产生额外 LLM 成本的路由投影 `INTERVIEW_HARNESS_SHADOW`。
- 开关关闭或任务未命中灰度时，显式按钮携带的自然语言 Prompt 仍可由旧 Harness 处理；新节点异常时只读降级，不影响简历功能。
- 新增 `AGENT_CHECKPOINT_RETENTION_DAYS`，启动时按完整控制线程清理过期 checkpoint；任务/项目删除仍即时清理，对话业务记忆不随 checkpoint 保留策略删除。
- 已覆盖 checkpoint 启动/写入故障、记忆版本冲突、非法 LLM JSON、重复请求和并发请求；错误事件不记录完整简历、附件或密钥。
- 新增认证后的 `/settings/harness-metrics` 进程内低基数计数器，观测拷打回合、只读降级、持久化/工作流错误和 checkpoint 清理数量，不包含用户、简历或消息标签。
- 生产部署拓扑、等价环境多进程/多实例压测、SQLite 参数或 PostgreSQL Saver 选择仍按用户要求后续确定。
- 旧 Harness、`compressed_context` 镜像双写和回退入口继续保留；停止双写与删除旧链路不属于本轮交付。

## 四、客观复核与剩余缺口

截至本轮，状态边界、分层记忆、业务状态机、结构化事实、前端恢复、开关灰度和自动验收均已落地，现有确认/撤回链路保持不变。仍存在且明确不属于本轮的内容：

- 生产采用多少 Worker、单容器还是多容器尚未确定，因此未在假定拓扑上给出生产容量结论。
- 当前仓库仍使用 SQLite Checkpointer；只有未来决定多容器/多服务器水平扩展时，才需接入共享 PostgreSQL Saver。
- 旧 `compressed_context` 镜像双写、旧压缩兼容入口和旧 Harness 仍保留，以满足本轮“必须可回退”的要求。
- shadow 当前比较确定性路由/状态投影，不双倍调用 LLM；若未来需要比较新旧回答质量，应在明确成本与隐私采样规则后单独实施。
- 自动验收已完成；追问质量、建议自然度和界面主观体验仍需用户人工验收。

本轮没有把“部署可访问”误判为“多进程写入已被证明安全”，也没有以历史 6 Worker 配置约束未来部署方案。

## 五、实施注意事项

- 先拆分状态边界，再启用 Checkpointer，禁止直接持久化当前完整 `AgentState`。
- 数据库中的简历版本必须高于历史对话和 checkpoint 状态。
- 已验证事实需保存来源或用户原始回答，禁止模型补造数字、技能和经历。
- 不长期保存图片、PDF Base64 或其他大附件；只保留必要的提取结果和引用信息。
- 新增数据库结构采用增量、可回滚方案，禁止破坏性迁移已有任务。
- 所有新 API 字段必须可选，旧前端和旧任务必须继续可用。
- 日志不得输出完整简历、附件、密钥或过量个人信息。
- 摘要失败、LLM 超时、结构化输出非法时必须安全降级，不得影响简历数据。

## 六、最终验收标准

- 现有后端测试、前端构建及核心人工回归全部通过。
- 普通对话、直接修改、复杂修改的效果与升级前一致。
- 未经用户确认，任何诊断或拷打内容均不会写入简历。
- 拷打过程可在刷新、暂停和服务重启后从原位置继续。
- 上下文压缩前后，当前问题、已验证事实、拒绝建议和未完成事项不丢失。
- 没有 JD 时不虚构岗位要求；没有证据时不生成虚假量化结果。
- 选择性接受、拒绝、撤回和过期候选保护均正常工作。
- 并发请求不会覆盖较新的简历或工作流状态。
- 不同用户、项目和岗位任务的上下文及 Checkpoint 完全隔离。
- 新 Harness 可通过开关回退；灰度结束后旧旁路能够被完整移除。

## 七、最终测试与验收计划

### 1. 测试原则

- 功能阶段完成后执行最终验收；本轮已按“模拟 LLM → 全量回归 → 真实 LLM → 浏览器 E2E”的顺序完成。
- 使用专用测试用户、项目、岗位任务和 JD，不使用真实用户数据。
- 先用可控模拟 LLM 做确定性契约测试，再使用真实 LLM 做效果冒烟测试。
- 每个问题必须先记录复现证据和根因，修复后新增回归测试并重跑相关套件。

### 2. 标准验收数据

- 一份包含薄弱项目描述“负责订单系统开发”的测试简历。
- 一份要求高并发、性能优化、个人贡献和量化结果的测试 JD。
- 另建一个同用户任务和一个不同用户任务，用于验证隔离。
- 测试前保存简历、记忆、checkpoint 和布局快照，便于比对副作用。

### 3. 核心端到端用例 `TC-INTERVIEW-001`

1. 用户选择或输入“开始拷打”；助手必须定位最高优先级短板，每轮只问一个问题。
2. 用户补充背景和个人动作，但不提供数字；助手必须继续追问证据，不得自行生成量化结果。
3. 用户补充“P95 从 800ms 降到 260ms”；系统必须把数字与用户原始回答关联为已验证事实。
4. 信息充分后生成“原文—建议稿—修改依据”；此时不生成修改确认、不写入简历。
5. 暂停拷打，刷新页面并重启后端，再继续；必须恢复同一模块、阶段、当前问题和已验证事实。
6. 用户明确要求“应用该建议”；系统复用现有确认预览，确认前数据库简历不变。
7. 选择性接受后只写入已选修改；执行撤回后简历与布局恢复到修改前快照。
8. 切换到其他任务和其他用户，不得读取本次拷打阶段、问题、事实或修改候选。

### 4. 自动化测试套件

- **基线回归**：现有全部后端测试、前端生产构建，以及普通对话/直接修改/复杂修改的 SSE 契约。
- **状态机契约**：合法/非法转移、单问题原则、暂停/继续/结束、幂等动作和异常降级。
- **事实安全**：简历事实、用户补充、模型推测的来源区分；无证据数字、技能、经历和 JD 要求不可写入。
- **持久化恢复**：页面刷新、进程重启、checkpoint 重建、摘要压缩和 checkpoint 故障注入。
- **修改安全**：应用前无写入，全部/选择性接受、拒绝、过期候选保护和撤回。
- **隔离与并发**：不同用户、项目和任务隔离；同任务并发不覆盖较新状态。
- **前端端到端**：模式入口、状态展示、暂停/继续、刷新恢复、确认预览和错误提示。

### 5. 部署与真实模型验证

- 开发阶段先执行部署无关的并发、幂等和重启恢复契约测试；生产拓扑确定后，再在等价环境记录 SQLite/共享 Saver 的锁等待、错误率和恢复结果。
- 如压测失败，先采取 WAL/`busy_timeout`、限制同任务并发或调整 Worker 等最小改动；若准备水平扩容，改用 PostgreSQL Saver 并重跑全部恢复/隔离测试。
- 真实 LLM 至少执行全面诊断、逐项拷打、无 JD、有 JD 和无量化证据五类冒烟案例，检查追问质量、事实忠实度和多次运行稳定性。

### 6. 缺陷收集与修复循环

- 每个缺陷记录测试编号、复现步骤、预期/实际结果、SSE/数据库/记忆/checkpoint 证据及影响范围。
- 修复时先添加能复现问题的测试，再修正状态转移、Prompt 或持久化逻辑，禁止通过放宽核心安全断言让测试“变绿”。
- 每轮修复后重跑受影响套件；所有问题关闭后再执行后端全量测试、前端生产构建和标准端到端用例。

### 7. 交付物与通过门槛

- 交付可重复执行的自动验收测试、专用测试数据和一份验收报告，报告列出通过项、缺陷、修复和剩余风险。
- 自动验收的硬门槛：现有回归全通过；新增拷打测试全通过；无未确认写入、无虚构事实、无跨用户/任务泄漏、无较新状态被覆盖；前端构建成功。
- 自动验收全部通过后，再由用户人工验收追问是否有针对性、建议是否自然、界面是否清晰及整体体验是否达标。

### 8. 后续部署与旧链路处理

- 具体生产拓扑、Worker 数量、SQLite/PostgreSQL Saver 选择和部署压测在功能验收后单独确定，不将历史 6 Worker 配置视为未来强制约束。
- 本次功能拓展保留旧 Harness、`compressed_context` 镜像双写和可回退入口，防止新链路异常时无法恢复。
- 只有在未来完成真实部署、灰度观察、数据校验和回退演练后，才能单独立项停止双写并删除旧链路；该清理不属于本次一次性实施范围。

### 9. 本轮自动验收报告（2026-08-05）

结果：**部署项除外，自动验收通过，可进入人工验收。**

- 后端全量：`97 passed, 4 subtests passed`。
- 前端生产构建：Vite 7.3.6 成功转换 1631 个模块；仅保留既有的大分块告警。
- 确定性专项：覆盖事实逐字来源、数字来源、单问题、暂停/继续/结束、非法动作安全处理、建议陈旧校验、原确认预览复用、重复 `request_id` 幂等、8 个并发回合不丢失、用户/任务隔离、重启恢复、过期 checkpoint 清理、非法 LLM 输出和持久化故障。
- 真实 LLM 合成数据：五类用例全部通过，包含全面诊断、逐项拷打、无 JD、带 JD、无量化证据；无数字回答未产生数字事实，量化回答提取 3 条逐字事实并进入 `awaiting_apply`。
- 浏览器 E2E：三入口正常；开始拷打后显示聚焦模块；暂停、刷新恢复、继续正常；合成回答提取 2 条事实并生成建议；点击“应用建议”后数据库保持不变；点击“全部接受”后才通过原链路保存；浏览器控制台无 error/warn。
- 修复的验收缺陷：只读节点曾导致当前简历原样重复写入，已改为拷打节点不向保存层回传业务更新并增加 API 回归测试；直接调用 `/chat` 时新增 Form 默认值兼容问题已修复；真实 LLM 非法 JSON 已验证为只读降级。
- 未执行：生产等价拓扑压力测试、SQLite/PostgreSQL 最终选型和旧链路删除；原因与边界见上一节。

可重复命令：

```bash
python -m pytest tests -q
cd frontend && npm run build
python scripts/interview_llm_smoke.py
```

### 10. 人工验收前补充修复（2026-08-06）

- 修复规范化空简历被误判为“已有内容”的问题；新建主简历会重新显示“从空白创建 / 导入现有简历”。
- 新建岗位版本增加“导入现有简历”，支持 JPG、PNG、PDF（最大 5MB）；解析请求明确携带新任务 ID，失败时回滚刚创建的空版本。
- 主简历原导入请求补齐任务 ID，避免多版本场景下写入错误任务。
- 本地 API 设置调整为“供应商（标识）→ URL → 模型 → Key”；按 OpenAI 兼容约定尝试 `GET {API 根地址}/models`，支持常见列表结构、已保存 Key 复用、查询失败提示和模型手填回退。
- 补充空简历识别与模型列表解析测试；更新后后端全量为 `101 passed, 4 subtests passed`，前端单测 3 项和生产构建通过。
- 浏览器 E2E 已验证主简历双入口、新版本三种来源、设置项顺序，以及模型列表不可用时保留手填值并显示回退提示；未保存或改写现有 API 配置。

### 11. 人工验收缺陷修复（2026-08-07）

- API 设置弹窗只允许通过右上角关闭按钮关闭，点击遮罩不再退出，降低误触风险。
- 模型输入移除浏览器原生 `datalist`，禁用自动填充并统一深色背景；查询结果使用弹窗内临时按钮展示，关闭弹窗即清空，不保存查询历史。
- 供应商注册表不再维护静态模型名或默认模型；模型列表根据用户填写的 URL 实时请求 `/models`。公开接口可直接查询，需要鉴权的接口在填写 Key 后重试；接口不支持时保留手动填写回退。
- 中国供应商文档改为官方中文页面；temperature 不再由用户统一设置，而由对话、结构化解析等任务角色分别管理，避免高温覆盖 JSON/PDF 解析器。
- PDF 解析按 Kimi 官方文件问答流程修正：对 `api.moonshot.cn` 先以 `purpose=file-extract` 上传文件，再读取 `/files/{file_id}/content` 的格式化抽取结果并作为 system message 交给模型，完成后尽力删除远端临时文件。Kimi Code 会员地址实测不提供内容获取接口，直接提示切换开放平台；其他原生支持 PDF 的兼容接口继续保留原始 `application/pdf` 传递方式，不再做缓慢的逐页图片回退。
- Windows 后端启动脚本增加 `--restart`；`start_local.cmd --restart` 会保持数据库与前端运行，只重启必须重载 Python 代码的后端。
- 更新后自动化结果：后端 `109 passed, 4 subtests passed`，前端单测 3 项及生产构建通过；新增 Kimi 文件上传、内容获取、远端清理和简历解析集成测试。浏览器实测“已获取 4 个可用模型”紧邻模型列表（间距约 6.4px），控制台无错误或警告。

### 12. 双 API 与统一网关最终实现（2026-08-08）

- 设置弹窗改为“对话 API / 解析 API”双页签；两边统一只填写 URL、模型和 Key，不再保留无调用价值的供应商字段。
- 网关以协议而非供应商名称适配，统一支持 OpenAI Chat、OpenAI Responses 与 Gemini Native；中转站上的 Gemini 仍按其实际暴露的 OpenAI 兼容协议调用，模型名只用于能力提示，不作为供应商判断依据。
- 对话 API 的连接测试同时检查普通回复、流式回复和结构化 JSON；解析 API 使用小型固定 PDF/PNG 检查连通、图像、原生 PDF 页面视觉、双栏布局、头像和结构化输出。
- 正式简历解析只走解析角色配置。未配置或未通过能力测试时明确阻止导入，不再静默改用对话模型或旧项目专用 URL。
- 主简历采用“解析为草稿 → 展示姓名/联系方式/经历数量 → 用户确认后写入”；失败时保留文件和错误信息，不覆盖原简历。岗位版本继续支持复制、空白和文件导入，导入失败会回滚新建空版本。
- PDF 与图片均把原始文件交给具备相应能力的模型；测试 PDF 为纯栅格页面，含文字、双栏、模拟头像和视觉校验码，避免只靠本地文本层通过伪测试。
- OpenAI 兼容解析请求启用 JSON 输出提示并提供足够的推理/输出预算；对不支持 `response_format` 的中转站自动退回同协议普通请求，对截断和非法 JSON 给出可操作错误。
- 旧项目只作为效果参考，不再作为运行时适配器或回退 URL；旧 Harness 仍按约定保留，解析 API 的旧供应商分支已被统一网关替代。

### 13. 最终自动验收报告（2026-08-08）

结果：**部署项除外，功能构建与自动验收全部通过，可进入人工验收。**

- 后端全量：`125 passed, 4 subtests passed`。
- 前端单测：`3 passed`；Vite 生产构建成功，保留既有的大分块体积提示，不影响功能。
- 真实解析 API 能力测试：PDF、图片、PDF 页面视觉、双栏布局、头像和结构化输出全部通过，单轮使用小型样例并并发测试 PDF/图片。
- 浏览器主简历 PDF E2E：选择文件、生成草稿、确认后写入、刷新持久化全部通过；模型基于导入经历生成了针对性追问。
- 浏览器岗位版本 PNG E2E：选择“导入现有简历”、创建独立版本、识别并展示姓名/联系方式/教育/工作/技能全部通过。
- 验收中修复两项真实缺陷：按钮点击事件被误当作确认数据导致请求未发送；推理模型输出预算不足导致 JSON 截断。两项均已增加回归保护。
- 浏览器验收创建的主简历和岗位版本已在测试完成后删除；未改动生产部署方案。

当前人工验收重点：设置交互是否直观、实际个人 PDF/图片识别准确度、拷打问题是否有针对性、建议是否真实自然，以及确认写入/撤回体验是否符合预期。

### 14. 解析稳定性缺陷复修（2026-08-08）

- 根因确认：此前 OpenAI Chat 适配器把 PDF 作为 `image_url` 数据地址发送。这是中转站非标准扩展，小型单页样例可能通过，但不能代表真实多页 PDF 的原生解析质量；图片 403 也来自该 Chat 路由的间歇性拒绝。
- Gemini 模型在自动模式下改为先探测同域名 Gemini Native `generateContent`；Apilio 的 `/v1` OpenAI 地址已实测可映射到 `/v1beta` 原生接口，PDF/图片均使用 `inline_data`。
- 解析能力测试新增“PDF 原生通道”硬门槛；OpenAI Chat 即使偶然读出测试 PDF，也不再标记为解析 API 验证通过。OpenAI Responses 的 `input_file` 和 Gemini Native 才属于可验证的原生文件通道。
- 图片与 PDF 能力测试由并发改为顺序，避免测试自身触发中转站限流；瞬时 403/429/5xx 有限重试，最终错误按权限、大小、额度和上游故障分类展示。
- 对话 API 测试移除 64 token 与三请求并发，改为顺序验证结构化普通回复和流式回复；同一 Apilio 配置连续三次完整通过，延迟约 4.7—16.4 秒。
- 正式解析 Prompt 强化页面阅读顺序、逐条 details、日期数字和学术字段映射；草稿新增实际解析通道与文件校验值，便于确认没有复用旧结果。
- 已有简历的“编辑”菜单新增“重新导入简历”，解析草稿确认前不覆盖当前内容，用户无需删除整个项目即可用新通道重新解析。
- 最终结果：后端 `127 passed, 4 subtests passed`；前端 `3 passed`；生产构建通过。真实 Gemini Native 能力测试连续两轮 8 项全通过，正式 PDF Schema 提取通过；页面实测显示 Gemini Native 和 PDF 原生通道通过。

### 15. 简历无损提取与模板注入修复（2026-08-08）

- 根因确认：Gemini Native 能正确读取真实 PDF，但旧 `Resume` Schema 没有出生年月、研究方向、荣誉和未知栏目，导致识别内容在结构化校验和模板注入阶段丢失；原导出器的 Flex 分页及整段禁止断页又造成大块空白和日期裁切。
- 数据契约新增 `basics.birth_date`、`basics.additional_fields`、`research_interests`、`honors` 和 `custom_sections`；无法映射的原栏目必须进入自定义栏目，旧字段别名自动迁移，旧简历不强制补空字段，避免影响既有对话摘要和确认状态。
- PDF 与图片共用一份无损解析 Prompt；荣誉不再混入证书，编号职责确定性拆分为独立条目。OpenAI Chat 解析适配器明确拒绝非标准 PDF 数据地址，只允许 Gemini Native 或 OpenAI Responses 承担 PDF 解析。
- 编辑弹窗、解析草稿、前端预览、PDF 与 DOCX 模板均接入新增字段；自定义栏目可继续编辑。PDF 服务端改为先按受控栏目顺序组装普通文档流，再交给 WeasyPrint 分页，长工作/项目允许自然跨页，日期不再被裁切。
- 真实 PDF 回归：正确提取姓名、`2002.06`、2 段教育、2 条研究方向、2 项荣誉、1 段实习和 2 个项目；三段经历详情分别为 6/7/6 条，教育结束日期完整保留为 `2027.06`、`2024.06`。合成图片回归正确提取姓名、电话、邮箱和教育经历。
- 真实解析结果使用紧凑模板导出为 1 页；逐页检查无大块空白、无日期裁切、研究方向与荣誉均可见。保留原 PDF 版式的双轨模式按约定留作后续工作，本阶段只完成信息提取与项目模板注入。
- 最终自动回归：后端 `132 passed, 4 subtests passed`，前端 `3 passed`，Vite 生产构建通过；浏览器只读验收确认新增字段均可编辑，控制台无错误或警告。嵌入式头像的自动裁剪与原版式保留一并留作后续，当前仍可手动上传头像。

### 16. 导入交互、语义排版与模块排序完善（2026-08-09）

- 简历解析成功后不再自动调用 LLM，也不再出现“处理中”或未经用户提问的分析回复；仅显示本地欢迎语与快捷入口，不写入对话记忆。欢迎语覆盖简历检查、针对性拷打、JD 匹配、内容修改和排版，并提示头像应自行上传清晰原图。
- 导入确认框只展示姓名及教育/工作/项目数量；隐藏解析协议、通道、文件校验值等技术信息。头像提示保留为一句操作建议，不尝试自动裁剪可能模糊的嵌入式头像。
- 工作经历与项目经历统一使用语义内容块：简介作为普通段落，职责标题加粗，职责明细使用 `(1)…` 编号，避免“标题带圆点”和“已有编号再套圆点”。前端预览、编辑保存、PDF 与 DOCX 链路保持一致；旧 `details` 数据会自动迁移。
- “专业技能”成为正式独立模块，常见的“技能/技术栈/技能特长”原始栏目自动归并，不再落入笼统的“其他”；剩余证书与语言栏目统一命名为“证书与语言”。
- 项目角色缺失时保持空白，前端、PDF 和 DOCX 均不再补“角色/项目”等占位词。
- 默认模板升级为紧凑高对比样式：正文黑色、重点加粗，默认字号 10.5pt、行距 1.32、模块间距 0.45rem、上下页边距 8mm、左右页边距 9mm；仅迁移仍使用旧默认值的布局，用户已自定义的间距继续保留。
- 排版设置新增模块拖拽与上下箭头排序，保存到当前岗位任务的布局配置；新增“根据 JD 推荐排序”，由现有 Agent 结合当前 JD/目标岗位生成临时排序预览，仍需用户确认后保存，不建立第二套修改机制。
- 修复前端分页曾额外预留 120px 导致“后端 PDF 一页、页面预览两页”的不一致；改为 8px 安全余量并保留溢出复核。同一份真实导入简历现已在页面与服务端 PDF 中均显示为 1 页 A4。
- 最终自动回归：后端 `136 passed, 4 subtests passed`；前端 `11 passed`；Vite 生产构建通过（仅保留既有的大分块提示）。真实数据 PDF 已渲染逐页检查，浏览器已确认黑色高对比、语义编号、专业技能、1/1 页和模块排序入口。

本轮人工验收重点：重新导入时确认框是否足够简洁；导入后是否只显示欢迎语而无自动回复；工作/项目语义分点是否符合预期；模板是否保持一页且清晰；手动排序持久化及 JD 排序建议的确认流程是否自然。

### 17. 原栏目边界、编号列表与排序交互复修（2026-08-09）

- 简历解析改为优先遵循原文件的可见栏目边界：原“专业技能/技能特长/技术栈”下的 CET、语言、证书或认证仍写入 `others.skills`；只有原文件存在独立证书或语言栏目时，才写入对应数组，禁止按语义把一个原栏目拆成多个新栏目或重复提取。
- 对旧解析数据增加保守兼容：技能原文明确出现“英语/语言/CET”等标签时，将被旧解析器拆出的语言项归回专业技能。当前真实简历中的“英语 CET-4”已回到专业技能，不再单独出现“证书与语言”。
- 专业技能、研究方向、荣誉及自定义栏目统一识别原生编号；以 `1.`、`(1)`、`（1）`、`一、` 等开头的条目保留自身编号且不再叠加圆点，无编号的多条描述继续使用圆点。前端、PDF 与 DOCX 行为一致。
- 排版浮层不再直接罗列全部模块，仅保留“调整模块顺序”按钮；点击后打开独立定高弹窗，列表区可滚动，不再溢出视口。
- 模块拖动改为根据鼠标位于目标行上半区/下半区实时决定插入位置，并在弹窗边缘自动滚动；同时保留上下箭头，支持两个方向调整，结束拖动后仅保存一次。
- 删除“根据 JD 推荐排序”专用入口及其固定 Prompt。用户仍可在普通对话中询问 Agent 的排序建议，再在模块排序弹窗中手动调整，避免为低频操作维护一条不稳定的专用修改链路。
- 前端布局归一化新增合法模块白名单，模型返回的未知模块 ID 会直接丢弃。另修复系统元数据 `parsing_status=completed` 被无损兜底误当成自定义简历栏目的问题；该异常栏目现已从页面和导出中消失。
- 最终回归：后端 `139 passed, 4 subtests passed`；前端 `13 passed`；生产构建通过。浏览器实测排版菜单高度正常、排序弹窗完整显示、上下箭头双向保存；真实简历页面与服务端 PDF 均确认 CET-4 位于专业技能、编号项无重复圆点、异常元数据栏目消失，PDF 为 1 页 A4。

### 18. 排序弹窗与功能措辞调整（2026-08-09）

- 模块排序改为桌面端左侧窄面板，缩短模块行宽并降低背景遮罩，拖动时可直接观察右侧 PDF 的实时变化；移动端仍居中显示并保留较深遮罩，保证可读性。
- 用户可见的“拷打”统一改为更专业的“深度打磨”，包括欢迎语、快捷入口、工作流状态、暂停/继续/结束反馈和 API 设置说明；内部仍保留 `coaching` 标识及旧说法识别，兼容历史会话和用户自然输入。
- “优化排版”改名为“排版建议”：含义覆盖字号、间距、模块样式和模块顺序，本入口默认只做分析与建议，不自动修改；模块顺序的实际调整继续由“排版 → 调整模块顺序”负责。
- 自动回归：后端 `139 passed, 5 subtests passed`；前端 `15 passed`；Vite 生产构建通过。

### 19. 设置弹窗、Kimi 连接与局部可见性复修（2026-08-09）

- 模块排序面板不再使用固定左边距；桌面端按“156px 岗位版本栏 + 39% 对话区”计算可用区域，面板右边框与对话/PDF 分割线精确重合。1280px 页面实测两者坐标均为 `594.35px`，右侧 PDF 完整露出。
- 对话网关增加模型温度约束：Kimi/K3 系列无论对话角色原温度是多少，均发送 API 要求的 `temperature=1`；连接测试也使用同一规则，并兼容上游返回 `only 1 is allowed` 后自动修正重试。当前已保存的 Moonshot `kimi-k2.6` 配置真实验证通过普通对话、流式输出和结构化输出。
- API SETTINGS 使用视口感知的固定高度，上下至少保留 20px，头部和底部固定，中间内容独立滚动；720px 高页面实测弹窗为 680px、上下各 20px。
- 模型查询状态与连接测试状态拆分。“已获取 N 个可用模型”固定显示在模型列表下方、API Key 上方；关闭再打开设置时清除模型列表、查询提示、连接结果和能力检查，不影响已保存的有效配置。
- 所有内部弹窗输入框统一声明深色配色并覆盖浏览器自动填充背景；已存在的简历名称被浏览器匹配时不再变白。空简历提示文字和“编辑简历”按钮提高边框、文字与底色对比度。
- `start_backend_local.cmd` 默认自动判断：后端未运行则启动，已经运行则终止旧进程并重新启动；`--restart` 继续兼容，另保留 `--start-only` 作为仅启动回退。实测无参数运行时 PID 从 `17404` 更新为 `16960`。
- 最终回归：后端 `139 passed, 5 subtests passed`；前端 `20 passed`；Vite 生产构建通过。浏览器实测 API 弹窗尺寸、模型状态位置、重开清空、深色项目名称输入框和排序面板边界均符合预期。
