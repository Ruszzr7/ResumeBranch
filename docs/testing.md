# ResumeBranch 功能测试回归清单

本文既是 Release 1 的人工回归模板，也是自动化测试入口说明。勾选项默认留空，不代表当前版本未实现；发布前应在目标环境重新执行并记录结果。代码测试通过不能替代真实浏览器、真实 Chromium、MySQL 或第三方 API 的专项验收。

## 测试环境

- **本地模式**：`APP_MODE=local` + SQLite，`http://127.0.0.1:5173`
- **多人模式**：`APP_MODE=multi_user` + MySQL，Windows 原生或 Docker 自托管地址
- **测试账号**：多人模式准备管理员与两个普通账号
- **浏览器**：当前稳定版 Chrome 或 Edge
- **测试日期**：

---

## 零、双运行模式测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 |
|------|----------|----------|----------|----------|
| 0.1 | 本地默认入口 | 使用 `scripts/start_local.cmd` 启动 | 无登录页；`/app/config` 返回 local + sqlite；仅监听回环地址 | ☐ |
| 0.2 | SQLite 持久化 | 创建简历，停止项目并重启电脑/服务 | `data/resumebranch.db` 仍在，简历可继续读取 | ☐ |
| 0.3 | 本地账号接口关闭 | 请求登录、注册和邀请码接口 | 均返回 404，页面不出现账号入口 | ☐ |
| 0.4 | 多人入口 | 使用多人 Compose 启动并访问首页 | 未登录跳转登录页；配置返回 multi_user + mysql | ☐ |
| 0.5 | 配置边界 | 在页面中查找模式开关 | 不存在运行时切换；模式只能通过部署配置选择 | ☐ |
| 0.6 | 数据源隔离 | 分别启动本地 SQLite 与多人 MySQL | 两端数据互不覆盖，不自动迁移 | ☐ |

---

## 一、用户系统测试（多人模式）

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 1.1 | 用户注册-正常流程 | 1. 进入注册页面<br>2. 输入有效邀请码<br>3. 填写邮箱、密码<br>4. 点击注册 | 注册成功，跳转登录页或自动登录 | ☐ | |
| 1.2 | 用户注册-无效邀请码 | 1. 输入无效/已使用的邀请码<br>2. 填写其他信息 | 提示"邀请码无效" | ☐ | |
| 1.3 | 用户登录-正常流程 | 1. 输入正确邮箱和密码<br>2. 点击登录 | 登录成功，跳转首页 | ☐ | |
| 1.3a | 管理员专用账号登录 | 1. 管理员输入非邮箱账号和正确密码<br>2. 普通用户尝试使用非邮箱标识 | 管理员登录成功；普通用户被拒绝并提示使用邮箱 | ☐ | |
| 1.4 | 用户登录-错误密码 | 1. 输入正确邮箱，错误密码 | 提示"密码错误" | ☐ | |
| 1.5 | 用户登录-未注册账号 | 1. 输入未注册的邮箱 | 提示"用户不存在" | ☐ | |
| 1.6 | 用户登出 | 1. 点击登出按钮 | 清除登录状态，跳转登录页 | ☐ | |
| 1.7 | Token过期处理 | 1. 等待默认24小时，或在测试配置中缩短有效期<br>2. 尝试访问需要认证的页面 | 显示登录已失效提示并跳转登录页 | ☐ | 有效期可由环境变量调整 |
| 1.7a | 单账号单活跃会话 | 1. 设备A登录<br>2. 设备B登录同一账号<br>3. 设备A继续请求 | 设备A提示重新登录；设备B正常；同一浏览器多标签页不互踢 | ☐ | |
| 1.8 | 邀请码获取 | 1. 登录管理员账号<br>2. 进入管理后台<br>3. 创建新邀请码 | 成功生成邀请码 | ☐ | |
| 1.9 | 管理员权限 | 1. 普通用户直接请求邀请码和 API 设置管理接口<br>2. 分别查看普通用户与管理员顶部菜单 | 越权请求返回 403；普通用户不显示管理入口；管理员显示邀请码和“API 设置”入口 | ☐ | |
| 1.10 | 邀请码原子消费 | 1. 使用同一邀请码快速发起两次注册 | 只有一个账号创建成功，不出现未消费邀请码或半成品用户 | ☐ | |

---

## 二、简历管理测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 2.1 | 创建新简历 | 1. 登录后进入首页<br>2. 选择"创建新简历" | 成功创建空白简历 | ☐ | |
| 2.2 | 上传简历图片/文件 | 1. 点击上传简历按钮<br>2. 选择图片或PDF文件<br>3. 等待解析 | 简历内容被提取并显示 | ☐ | 支持JPG/PNG/PDF |
| 2.3 | 简历OCR解析状态轮询 | 1. 上传简历后<br>2. 观察解析状态 | 显示"解析中"→"解析完成" | ☐ | |
| 2.4 | 简历预览-A4分页 | 1. 编辑简历内容<br>2. 切换到预览模式 | 正确显示A4分页效果 | ☐ | |
| 2.5 | 简历预览-样式调节 | 1. 调整页边距、字体大小 | 预览实时更新 | ☐ | |
| 2.6 | 简历编辑-基本信息 | 1. 编辑姓名、性别、手机、邮箱、期望岗位 | 保存成功 | ☐ | |
| 2.7 | 简历编辑-教育经历 | 1. 添加教育经历<br>2. 填写学校、专业、学历、时间 | 保存成功 | ☐ | |
| 2.8 | 简历编辑-工作经历 | 1. 添加工作经历<br>2. 填写公司、职位、时间、工作内容 | 保存成功 | ☐ | |
| 2.9 | 简历编辑-项目经历 | 1. 添加项目经历<br>2. 填写项目名、角色、时间、项目内容 | 保存成功 | ☐ | |
| 2.10 | 简历编辑-技能证书 | 1. 添加技能、证书、语言 | 保存成功 | ☐ | |
| 2.11 | 简历编辑-自我评价 | 1. 添加自我评价 | 保存成功 | ☐ | |
| 2.12 | 简历照片上传 | 1. 上传证件照 | 照片正确显示在简历上 | ☐ | Base64格式 |
| 2.13 | 简历数据持久化 | 1. 编辑简历后刷新页面 | 数据仍然保存 | ☐ | |
| 2.14 | 多简历管理 | 1. 创建多个简历<br>2. 切换不同简历 | 正确切换，数据独立 | ☐ | |
| 2.15 | 默认排版防误触 | 1. 点击恢复默认排版<br>2. 先取消，再重新点击并确认 | 取消时排版不变；确认后才恢复默认值 | ☐ | |
| 2.16 | 中英文简历与翻译复用 | 1. 将中文简历切换为英文<br>2. 切回中文后再次切换<br>3. 修改一处中文内容后再次翻译 | 结构保持不变；第二次可复用完整结果；只修改一处时其余字段复用缓存 | ☐ | 对话 API 必须可用 |
| 2.17 | 分角色字号弹窗 | 1. 打开“排版”，点击原“正文字号”位置的“调整文字大小”按钮<br>2. 分别调整姓名、模块标题、条目标题、元信息、正文和标签<br>3. 取消后重开，再调整并应用 | 原正文字号滑杆被同款排版按钮替代并打开六类字号弹窗；选项按 0.5pt 递增；预览实时更新；取消不保存；应用后持久化且旧布局自动兼容 | ☐ | 超过两页时禁止应用 |
| 2.18 | 任意文字局部加粗 | 1. 在单行或多行字段中选中文字并按 Ctrl+B<br>2. 保存并重新打开编辑弹窗 | 命令执行后选区立即收起，便于辨认加粗结果；仅选中文字加粗；其余文字、字号和内容不变；重新编辑时不暴露底层标记 | ☐ | 加粗提示仅在编辑弹窗顶部显示一次；不支持颜色、斜体和下划线 |
| 2.19 | 栏目名称与段落标记 | 1. 将“专业技能”改为“技术栈”<br>2. 分别选择无标记、分点、编号<br>3. 刷新并导出 | 名称和标记持久化；预览/PDF/DOCX 一致 | ☐ | 设置只作用于当前简历 |
| 2.20 | 栏目并入教育经历 | 1. 分别将研究方向、主要荣誉、论文、证书与语言并入教育经历<br>2. 恢复为独立栏目 | 并入后显示为教育经历内的子栏目且不重复显示；恢复后回到模块顺序中的独立位置 | ☐ | 内容数据本身不移动，仅改变展示位置 |
| 2.21 | 论文与学校标签 | 1. 添加论文内容<br>2. 为学校添加 `211`、`双一流`<br>3. 检查三端 | 论文可独立显示或并入教育经历；学校显示为 `学校 · 211 · 双一流` | ☐ | 不保存论文专用复杂结构 |
| 2.22 | 经历内容形式 | 1. 在项目经历中填写技术栈、项目简介、项目职责和其他项目内容<br>2. 分别将技术栈、项目简介、项目职责设为段落、分点、编号<br>3. 检查预览并导出 | 项目内容按“技术栈→项目简介→项目职责→其他项目内容”显示；段落、分点和编号换行显示；标题与内容均使用正文字号，页面/PDF/DOCX 一致；顶层“技术栈”仍属于专业技能栏目 | ☐ | 标题仍可通过 Ctrl+B 单独加粗 |
| 2.23 | 栏目设置实时预览 | 1. 打开栏目设置<br>2. 修改栏目名称、分点形式和栏目位置<br>3. 观察右侧后关闭，再重新打开并应用 | 修改立即反映在右侧预览；关闭不保存；应用后持久化；未设置的位置明确显示“独立栏目”；弹窗在可视区域内无溢出 | ☐ | |
| 2.24 | 多个自定义栏目独立排序 | 1. 新增自定义栏目 A 和 B 并填写内容<br>2. 打开“模块顺序”<br>3. 将 A、B 分别拖动到不同位置并应用<br>4. 刷新并检查预览、PDF、DOCX | 排序列表显示 A、B 的实际标题，而不是合并显示“自定义栏目”；三端按相同顺序渲染；旧布局中的单个“自定义栏目”仍可正常显示 | ☐ | |

---

## 三、岗位JD管理测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 3.1 | JD文本粘贴 | 1. 选择文本输入模式<br>2. 粘贴JD文本<br>3. 点击解析 | 正确解析为结构化数据 | ☐ | |
| 3.2 | JD图片识别 | 1. 选择图片模式<br>2. 上传JD截图 | OCR识别并解析 | ☐ | |
| 3.3 | JD表单编辑 | 1. 编辑公司、职位、部门等信息 | 保存成功 | ☐ | |
| 3.4 | JD预览 | 1. 切换到预览模式 | 正确显示JD内容 | ☐ | |
| 3.5 | JD与简历关联 | 1. 同时加载简历和JD<br>2. 与AI对话时输入JD内容 | AI能同时参考简历和JD | ☐ | |

---

## 四、AI对话功能测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 4.1 | 发送消息 | 1. 在输入框输入内容<br>2. 点击发送或按Enter | 消息发送到服务端 | ☐ | |
| 4.2 | SSE流式响应 | 1. 发送消息<br>2. 观察AI回复 | 文字逐字流式显示 | ☐ | 核心功能 |
| 4.3 | AI对话-简历优化建议 | 1. 输入"帮我优化简历"类似内容 | AI给出优化建议 | ☐ | |
| 4.4 | AI对话-深度挖掘 | 1. 回答AI的追问<br>2. 补充经历细节 | AI继续追问或给出建议 | ☐ | STAR法则 |
| 4.5 | 简历修改确认流程 | 1. 让AI修改简历<br>2. 观察确认弹窗 | 弹出确认框 | ☐ | |
| 4.6 | 确认保存 | 1. 在确认弹窗点击"确认" | 简历数据被保存 | ☐ | |
| 4.7 | 取消保存 | 1. 在确认弹窗点击"取消" | 简历数据不被保存 | ☐ | |
| 4.8 | 对话历史保存 | 1. 进行多轮对话<br>2. 刷新页面 | 对话历史被保留 | ☐ | |
| 4.9 | 上下文压缩 | 1. 进行超过20轮对话<br>2. 观察对话是否流畅 | 上下文被压缩，对话继续 | ☐ | |
| 4.10 | Markdown渲染 | 1. 观察AI回复格式 | Markdown正确渲染 | ☐ | 列表、加粗等 |
| 4.11 | 加载简历上下文 | 1. 对话中提到简历内容 | AI能读取当前简历 | ☐ | |
| 4.12 | 加载JD上下文 | 1. 对话中提到JD内容 | AI能读取当前JD | ☐ | |
| 4.13 | 对话式局部加粗 | 1. 输入“把‘唯一原文’加粗”<br>2. 先取消预览，再重新操作并确认<br>3. 使用重复原文或不存在原文重试 | 唯一命中时只生成临时预览，确认后保存；取消不保存；零命中或多命中拒绝修改且不调用模型 | ☐ | 取消加粗遵循同一流程 |
| 4.14 | 直接修改保留格式 | 1. 对原本加粗或含局部格式的字段执行明确文本替换<br>2. 比较修改前后预览与数据 | 只替换目标文本，原字段粗体及其他格式不丢失 | ☐ | 不经过 LLM 的确定性路径 |
| 4.15 | 过期预览恢复正式版本 | 1. 生成修改预览<br>2. 在另一窗口修改同一简历<br>3. 回到预览弹窗确认 | 保存被拒绝并显示中文过期提示；弹窗关闭后右侧立即恢复数据库中的最新正式简历，无需刷新页面 | ☐ | |

---

## 五、PDF导出测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 5.1 | 服务端PDF导出 | 1. 点击导出PDF按钮<br>2. 等待生成 | 本地版保存到输出目录；多人版下载PDF文件 | ☐ | 统一使用 Chromium，缺失或执行失败时明确报错 |
| 5.2 | PDF内容完整性 | 1. 打开导出的PDF<br>2. 检查内容 | 包含所有简历信息 | ☐ | |
| 5.3 | PDF排版 | 1. 检查PDF排版 | 符合A4页面，布局合理 | ☐ | |
| 5.4 | PDF照片显示 | 1. 简历包含照片<br>2. 导出PDF | 照片正确显示 | ☐ | |
| 5.5 | AI 页面快照同源性 | 1. 对需要判断分页的问题触发简历快照技能<br>2. 与正式 PDF 对比 | 快照由同一 PDF 渲染链路生成，页数和内容一致；快照不写入业务数据 | ☐ | 需要 Poppler，默认最多读取有限页数 |
| 5.6 | 大简历PDF导出 | 1. 简历内容较多<br>2. 导出PDF | 多页PDF正确生成 | ☐ | |
| 5.7 | 三端排版一致性 | 1. 调整字号、行距、模块间距和页边距<br>2. 分别检查预览、PDF、DOCX<br>3. 检查中英文混排 | 三端使用中文微软雅黑、英文 Arial；页数、模块层级、主要换行和边距一致，无日期溢出 | ☐ | DOCX保持可编辑，不要求像素级一致 |
| 5.8 | 分角色字号与局部粗体一致性 | 1. 设置六类字号<br>2. 在中英文标题、标签和正文中加粗或取消加粗<br>3. 分别检查一页和两页的预览、PDF、DOCX | 三端字号层级、粗体范围、换行和页数尽可能一致；DOCX 粗体仍为可编辑 Run；空模块、日期右对齐和悬挂缩进不退化 | ☐ | 当前协议为 schema v9 |
| 5.9 | 中英文标点与字体归属 | 1. 输入 `，。；：（）` 与 `,.;:!?()`<br>2. 混排中文、英文、数字、粗体和 `RL/MPIC` 等缩写<br>3. 检查预览、PDF、DOCX 的原文、换行和字体 | 不改写用户输入的全角/半角码位；ASCII 字母、数字及半角标点使用 Arial，汉字及全角中文标点使用微软雅黑；预览与 PDF 主要换行一致，Word 页数不增加 | ☐ | Word 可编辑优先，不要求逐像素一致 |
| 5.10 | 双模式导出策略 | 1. 本地版重复导出同一简历<br>2. 检查浏览器下载目录和 `output/resumes`，点击“打开导出文件夹”<br>3. 多人版执行相同操作 | 文件名为“简历组_版本_日期”；本地仅保留项目文件且重名增加 `_01`，系统文件管理器打开正确目录；多人版仅浏览器下载 | ☐ | |

---

## 六、UI/UX测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 6.1 | 响应式布局-桌面端 | 1. 在1920px宽度访问 | 正常显示 | ☐ | |
| 6.2 | 响应式布局-平板 | 1. 在1024px宽度访问 | 正常显示 | ☐ | |
| 6.3 | 响应式布局-手机 | 1. 在375px宽度访问 | 显示移动端布局 | ☐ | |
| 6.4 | 移动端底部导航 | 1. 在手机端访问<br>2. 观察底部导航栏 | 导航栏显示 | ☐ | |
| 6.5 | 模块高亮动画 | 1. AI处理中<br>2. 观察模块 | 蓝紫色发光效果 | ☐ | |
| 6.6 | 加载状态-首次进入 | 1. 首次进入应用 | 显示加载动画 | ☐ | |
| 6.7 | 加载状态-AI响应 | 1. 发送消息等待回复 | 显示"正在处理" | ☐ | |
| 6.8 | 加载文案轮播 | 1. 等待AI响应 | 加载文案循环变化 | ☐ | |
| 6.9 | 富文本编辑-加粗 | 1. 选中文本<br>2. Ctrl+B | 文本加粗 | ☐ | |
| 6.10 | 日期选择器 | 1. 选择日期<br>2. 选择"至今" | 日期正确显示 | ☐ | |
| 6.11 | 图片预览 | 1. 点击图片<br>2. 观察预览弹窗 | 弹窗显示大图 | ☐ | |
| 6.12 | 弹窗过渡动画 | 1. 打开/关闭弹窗 | 平滑过渡效果 | ☐ | |
| 6.13 | 窄屏字号弹窗 | 1. 在 375px 和 964px 宽度打开字号弹窗<br>2. 修改各角色字号并滚动<br>3. 检查取消、恢复默认和应用按钮 | 字段、说明和操作按钮完整可见且可操作，无横向溢出；实时预览不遮挡关键导航 | ☐ | |
| 6.14 | 简历工具栏顺序 | 1. 分别在桌面和窄屏查看简历顶部工具栏<br>2. 依次使用各按钮 | 顺序固定为调节大小、编辑、排版、中英文、原版、导出 PDF、导出 Word；视觉顺序与键盘焦点顺序一致 | ☐ | 无原版文件时仅隐藏原版按钮 |
| 6.15 | 正文预计行数 | 1. 打开编辑简历<br>2. 输入会自动换行的中英文与粗体内容<br>3. 使用接近换行临界点的项目简介<br>4. 检查标签独立成段的项目职责<br>5. 修改正文字号、标签字号、行距和左右页边距后重新打开编辑框 | 内容框右下方显示“简历约 N 行”；计数随内容自动换行和共享正文排版参数变化；行内标签参与同一行换行测量，独立成段的标签不计入正文行数；顶部说明明确这是预计行数 | ☐ | PDF/DOCX 字体引擎可能产生细微差异 |
| 6.16 | 正文双端对齐与标签/内容字重 | 1. 查看中英文长段落、分点和项目简介<br>2. 打开编辑简历<br>3. 使用独立按钮取消项目经历固定标签的加粗<br>4. 在工作经历内容框中选中“项目简介”等任意文字并按 `Ctrl+B`<br>5. 保存后检查预览、PDF、DOCX | 正文类内容三端双端对齐，标题、日期和联系信息维持原对齐；项目经历固定标签默认加粗并由 `label_bold` 独立控制；工作经历不按文字前缀生成固定标签，任意文字均通过正文 `**` 协议切换字重 | ☐ | 工作内容即使以“项目简介：”开头也必须保持普通可编辑正文 |
| 6.17 | 多用户暗色主题一致性 | 1. 打开登录页、注册页和邀请码弹窗<br>2. 触发浏览器历史填写建议<br>3. 在桌面与窄屏查看账号菜单 | 页面、弹窗和可控输入状态保持暗色主题；账号菜单不显示虚构头像或首字符头像；窄屏不出现头像占位 | ☐ | 浏览器自身不可定制的原生建议面板除外 |

---

## 七、安全测试（多人模式）

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 7.1 | 未授权访问 | 1. 未登录直接访问首页 | 跳转登录页 | ☐ | |
| 7.2 | 越权访问-他人数据 | 1. 登录账号A<br>2. 尝试访问账号B的简历 | 无权限或返回空 | ☐ | |
| 7.3 | SQL注入防护 | 1. 在输入框输入SQL语句 | 无异常或被拦截 | ☐ | |
| 7.4 | XSS防护 | 1. 输入HTML/JS脚本 | 脚本被转义 | ☐ | |
| 7.5 | 敏感信息泄露 | 1. 检查网络请求<br>2. 检查响应 | 无敏感信息泄露 | ☐ | |
| 7.6 | JWT 启动保护 | 清空或使用示例 `JWT_SECRET_KEY` 启动多人后端 | 后端拒绝启动并给出明确配置错误 | ☐ | |
| 7.7 | CORS 允许列表 | 从未配置的跨域来源调用接口 | 浏览器不授予跨域访问 | ☐ | |

---

## 八、性能测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 8.1 | 页面加载时间 | 1. 打开首页<br>2. 记录加载时间 | < 3秒 | ☐ | |
| 8.2 | AI响应时间 | 1. 发送消息<br>2. 记录首次响应时间 | < 10秒 | ☐ | |
| 8.3 | PDF生成时间 | 1. 点击导出PDF<br>2. 记录生成时间 | < 15秒 | ☐ | |
| 8.4 | 大量数据渲染 | 1. 创建多段经历<br>2. 观察渲染 | 无明显卡顿 | ☐ | |
| 8.5 | 修改并发兜底 | 在主对话与命令对话同时请求修改同一简历 | 仅一个请求进入预览生成；另一个收到中文占用提示；普通咨询仍可进行 | ☐ | |
| 8.6 | 修改状态灯 | 生成预览、等待确认、完成/取消各观察一次 | 所属对话标签依次显示蓝色呼吸灯、琥珀色常亮灯并在结束后消失 | ☐ | |

---

## 九、部署相关测试

| 序号 | 测试功能 | 测试步骤 | 预期结果 | 测试状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 9.1 | 服务健康检查 | 1. `POST /health` | 返回健康状态 | ☐ | |
| 9.2 | Compose Nginx 代理 | 1. 通过 `http://主机:8080` 访问并调用业务接口 | 静态页面与 HTTP/SSE 请求均正确代理到后端 | ☐ | |
| 9.3 | 静态资源加载 | 1. 检查CSS/JS/图片 | 正常加载 | ☐ | |
| 9.4 | 外部 HTTPS 入口 | 1. 在已配置反向代理的环境通过 HTTPS 访问 | 页面与流式对话正常，无混合内容 | ☐ | Compose 本身不提供 TLS |
| 9.5 | 日志记录 | 1. 执行操作<br>2. 检查日志 | 正确记录 | ☐ | |
| 9.6 | 本地启动无 MySQL 依赖 | 停止/卸载 MySQL 后运行 `start_local.cmd` | 前后端和 SQLite 正常启动 | ☐ | |
| 9.7 | 多人卷持久化 | Compose `down` 后重新 `up` | MySQL 用户/简历和应用数据卷内容仍在 | ☐ | 禁止使用 `down -v` |

---

## 自动化命令

```powershell
$env:APP_MODE = "local"
$env:LOCAL_USER_EMAIL = "local@localhost"
$env:DATABASE_URL = "sqlite:///./.local-run/test-suite.db"
$env:AGENT_CHECKPOINTER_ENABLED = "true"
$env:AGENT_CHECKPOINT_DB_PATH = ".local-run/test-checkpoints.sqlite"
.\.venv-win\Scripts\python.exe -m unittest discover -s tests

Set-Location frontend
npm test
npm run build
Set-Location ..

Remove-Item Env:APP_MODE, Env:LOCAL_USER_EMAIL, Env:DATABASE_URL, Env:AGENT_CHECKPOINTER_ENABLED, Env:AGENT_CHECKPOINT_DB_PATH
```

显式设置测试 SQLite 是必要的：后端初始化会读取当前环境；如果 `.env` 正处于多人模式，直接运行测试可能连接本机 MySQL。`.local-run/test-suite.db` 和测试检查点均为可再生、被 Git 忽略的测试文件。

默认测试会跳过需要真实 MySQL 的破坏性集成测试。如需在专用测试数据库中执行，先确认 `DATABASE_URL` 不指向正式或个人数据，再运行：

```powershell
$env:RUN_MYSQL_INTEGRATION = "1"
.\.venv-win\Scripts\python.exe -m unittest tests.test_mysql_multi_user_integration
Remove-Item Env:RUN_MYSQL_INTEGRATION
```

需要真实 LLM 的访谈链路冒烟测试不会进入默认 CI，配置测试专用 API 后可运行：

```powershell
.\.venv-win\Scripts\python.exe scripts\interview_llm_smoke.py
```

双模式核心回归集中在 `tests/test_runtime_profiles.py`，覆盖本地账号关闭、SQLite 持久化、导出碰撞、多用户管理员权限、邀请码事务和用户隔离。Docker 配置还应执行：

```bash
docker compose --env-file .env.docker -f docker-compose.multi-user.yml config
```

GitHub Actions 在 push 和 Pull Request 时自动执行后端测试、前端测试和生产构建；Docker 工作流另行验证 MySQL 多用户部署、权限隔离、PDF/快照渲染及卷持久化。CI 通过不替代本页涉及真实浏览器交互的人工验收。

---

## 测试总结

| 项目 | 数量 | 完成 | 未通过 | 备注 |
|------|------|------|--------|------|
| 双运行模式 | 6 | ☐ | ☐ | |
| 用户系统 | 12 | ☐ | ☐ | 仅多人模式 |
| 简历管理 | 23 | ☐ | ☐ | |
| JD管理 | 5 | ☐ | ☐ | |
| AI对话 | 15 | ☐ | ☐ | |
| PDF/DOCX导出 | 10 | ☐ | ☐ | |
| UI/UX | 17 | ☐ | ☐ | |
| 安全 | 7 | ☐ | ☐ | 重点为多人模式 |
| 性能 | 6 | ☐ | ☐ | |
| 部署 | 7 | ☐ | ☐ | |
| **总计** | **108** | ☐ | ☐ | |

---

## 缺陷记录

| 序号 | 缺陷描述 | 严重程度 | 发现时间 | 修复状态 | 备注 |
|------|----------|----------|----------|----------|------|
| 1 | | | | 待修复 | |
| 2 | | | | 待修复 | |
| 3 | | | | 待修复 | |

---

**测试人员**:
**测试日期**:
