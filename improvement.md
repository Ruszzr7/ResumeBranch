# 项目改进计划

### 1. 统一状态与错误交互

- 用统一弹窗和消息提示替换 `alert`、`prompt`，错误信息说明原因和解决办法。
- 解析、分析、保存和导出均显示明确进度，并提供重试或取消。
- 本地启动时检查后端、模型配置、字体和 PDF 能力，直接指出缺失项。

### 2. 保留 Vue 3，重构前端状态管理

- 继续使用 Vue 3 + Vite + Element Plus，无需更换 React/Nuxt。
- 将超大的 `App.vue` 拆为页面、业务组件和 composables；使用 Pinia 管理简历、岗位版本和会话状态。
- 封装统一 API Client，集中处理超时、错误和流式响应。

### 3. 简化 LangGraph 编排

- LangGraph 仅负责“分析 → 提案 → 用户确认 → 应用修改”等多步骤流程。
- 简历/JD 解析等单步任务直接调用结构化模型输出，避免所有请求都经过 Agent。
- 使用 Pydantic Schema 和工具调用校验结果，减少手工 JSON 解析及 Prompt 约束。

### 4. 统一预览与 PDF 渲染

- 抽取前端预览和 PDF 共用的排版变量、字体与分页规则。
- WeasyPrint 能稳定满足需求时继续保留；若预览与导出长期不一致，再改用 Playwright/Chromium 打印。
- 不同时维护多套 PDF 渲染逻辑。
