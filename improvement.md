# 项目体验与技术改进计划

> 定位：面向本地使用，优先改善核心求职流程和技术适配，不追求生产级基础设施。

## P0：使用体验

### 1. AI 修改可预览、可选择、可撤销

- AI 先生成修改建议和前后 Diff，支持逐条接受、全部接受或拒绝。
- 每次应用前自动创建快照，避免直接覆盖简历。

### 2. 强化“简历档案 → 岗位版本”流程

- 新建岗位版本时自动引导填写 JD，并继承主简历生成定向版本。
- 展示匹配证据、能力缺口和修改建议，不只给模糊总分。
- 支持岗位版本重命名、复制、归档、删除和最近使用排序。

### 3. 提升编辑与排版反馈

- 增加字段完整度、ATS 关键词、内容重复和表述空泛提示。
- 实时提示分页、溢出和空白过多，并提供“一键适配一页/两页”。
- 显示自动保存状态；恢复默认设置只重置排版，不影响内容。

### 4. 统一状态与错误交互

- 用统一弹窗和消息提示替换 `alert`、`prompt`，错误信息说明原因和解决办法。
- 解析、分析、保存和导出均显示明确进度，并提供重试或取消。
- 本地启动时检查后端、模型配置、字体和 PDF 能力，直接指出缺失项。

### 5. 加强本地数据体验

- 支持整个简历档案的备份、恢复和 JSON 导入导出。
- 明确显示数据保存位置，避免用户误删或重装后丢失数据。

## P1：技术选型与实现

### 6. 保留 Vue 3，重构前端状态管理

- 继续使用 Vue 3 + Vite + Element Plus，无需更换 React/Nuxt。
- 将超大的 `App.vue` 拆为页面、业务组件和 composables；使用 Pinia 管理简历、岗位版本和会话状态。
- 封装统一 API Client，集中处理超时、错误和流式响应。

### 7. 简化 LangGraph 编排

- LangGraph 仅负责“分析 → 提案 → 用户确认 → 应用修改”等多步骤流程。
- 简历/JD 解析等单步任务直接调用结构化模型输出，避免所有请求都经过 Agent。
- 使用 Pydantic Schema 和工具调用校验结果，减少手工 JSON 解析及 Prompt 约束。

### 8. 采用任务分级模型策略

- 普通对话和格式整理使用低成本模型，复杂分析或重写再升级模型。
- 在界面显示失败重试与降级结果；记录单次耗时和 Token，避免无感高消耗。

### 9. 保持当前 MySQL 数据库

- 项目已经迁移到 MySQL，继续使用 SQLAlchemy + PyMySQL + `utf8mb4`，不再切回 SQLite。
- 为本地数据增加一键备份、恢复和连接检查；保留 `DATABASE_URL` 配置方式。
- 数据结构继续演进时引入 Alembic；除非出现明确需求，否则无需再迁移 PostgreSQL。

### 10. 统一预览与 PDF 渲染

- 抽取前端预览和 PDF 共用的排版变量、字体与分页规则。
- WeasyPrint 能稳定满足需求时继续保留；若预览与导出长期不一致，再改用 Playwright/Chromium 打印。
- 不同时维护多套 PDF 渲染逻辑。

## 最小质量保障

- 固定一组简历与 JD 样本，验证解析、匹配和修改结果。
- 使用 Playwright 覆盖“导入简历、按 JD 优化、导出 PDF”三条核心流程。
- 对 PDF 做页面数和截图对比，防止排版回退。

## 暂不优先

- Redis、Celery、微服务、复杂消息队列和完整生产级监控。
- 再次更换 PostgreSQL、SQLite 或其他数据库。
- 在核心流程完善前继续增加大量模板、模型供应商或管理功能。
